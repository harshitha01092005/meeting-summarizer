{
  "name": "meeting-summarizer",
  "version": "1.0.0",
  "private": true,
  "description": "Transcribe meeting audio and turn it into decisions and action items.",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check public/app.js && node --test"
  },
  "license": "MIT"
}
